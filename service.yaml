apiVersion: v1
kind: Service

metadata:
  name: enterprise-rag-service

spec:
  selector:
    app: rag

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30001

  type: NodePort